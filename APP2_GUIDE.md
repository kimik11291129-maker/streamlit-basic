# 🤖 AI 채팅 어시스턴트 (app2.py) 사용 가이드

## 📋 개요
OpenAI API를 활용한 고급 채팅 어시스턴트입니다. 텍스트 대화뿐만 아니라 이미지 분석, 파일 처리 등의 기능을 제공합니다.

---

## 🚀 빠른 시작

### 1️⃣ OpenAI API 키 발급
- [OpenAI Platform](https://platform.openai.com/api-keys)에서 계정 생성
- API 키 발급 및 복사

### 2️⃣ 환경 변수 설정 (2가지 방법)

#### 방법 A: .env 파일 사용 (권장)
1. `.env.example`를 `.env`로 복사
   ```bash
   cp .env.example .env
   ```
2. `.env` 파일에 API 키 입력
   ```
   OPENAI_API_KEY=sk-your-actual-key-here
   ```

#### 방법 B: 앱에서 직접 입력
- 앱 실행 후 좌측 사이드바의 "OpenAI API 키" 필드에 직접 입력

### 3️⃣ 앱 실행
```bash
streamlit run app2.py
```
- 기본 포트: http://localhost:8502
- 앱이 열리면 API 키가 자동으로 로드되거나 입력한 후 사용 시작

---

## 💬 기본 채팅
1. 아래 "메시지 입력" 필드에 질문을 입력
2. Enter를 누르거나 전송 버튼 클릭
3. AI 어시스턴트의 답변이 채팅 히스토리에 표시

### 예시
- 일반 질문: "파이썬에서 리스트 컴프리헨션이 뭐야?"
- 코딩 도움: "이 코드를 최적화해줄 수 있어?"
- 창의적 작업: "샤를린 기행문을 써줘"

---

## 🖼️ 이미지 분석
1. 우측 "📎 파일 첨부" 패널의 "이미지 선택"에서 이미지 업로드
   - 지원 형식: JPG, PNG, GIF, WebP
2. 이미지가 미리보기에 표시됨
3. 메시지 입력란에 질문 입력 (예: "이 사진에서 뭐가 보여?")
4. 전송하면 AI가 이미지를 분석해 답변

### 예시
- "이 그래프가 무엇을 나타내고 있어?"
- "이 이미지에서 객체를 인식해줄 수 있어?"
- "이 레이아웃을 개선하려면 어떻게 해야 할까?"

---

## 📄 파일 처리
1. 우측 "📎 파일 첨부" 패널의 "텍스트 파일 선택"에서 파일 업로드
   - 지원 형식: TXT, MD, CSV, JSON
2. 파일이 첨부됨 표시
3. 메시지 입력란에 질문 입력
4. 전송하면 파일 내용을 기반으로 AI가 응답

### 예시
- CSV 분석: "이 데이터의 주요 트렌드를 요약해줄 수 있어?"
- 코드 리뷰: "이 코드의 버그가 있나 찾아줄 수 있어?"
- 문서 처리: "이 문서에서 핵심 내용을 3줄로 요약해줘"

---

## ⚙️ 사이드바 기능

### API 키 입력
- 환경 변수로 로드되지 않은 경우 여기서 입력
- 입력 후 자동으로 저장됨

### 대화 초기화
- "💬 대화 초기화" 버튼 클릭
- 모든 채팅 히스토리가 삭제되고 새로운 대화 시작

### 사용 가능 기능
- 사이드바 하단에 지원하는 기능 목록 표시

---

## 🔒 보안 주의사항

### API 키 관리
- ⚠️ `.env` 파일을 **절대로 공개하거나 git에 커밋하지 마세요**
- `.gitignore`에 `.env`가 포함되어 있는지 확인
- 공개 리포지토리에 올릴 때는 `.env.example`만 공유

### 비용
- OpenAI API는 사용량에 따라 비용 발생
- [API 사용량 모니터링](https://platform.openai.com/account/billing/overview)에서 정기적으로 확인

---

## 🐛 문제 해결

### "❌ OpenAI API 키가 설정되지 않았습니다" 에러
- 해결: 사이드바에서 API 키 입력 또는 `.env` 파일 확인

### "❌ 오류 발생: ..." 에러
- API 할당량 초과 확인
- 인터넷 연결 확인
- OpenAI API 상태 페이지 확인

### 이미지가 업로드되지 않음
- 지원 형식 확인 (JPG, PNG, GIF, WebP)
- 파일 크기 확인 (너무 크면 실패 가능)

### 파일 처리가 느림
- 큰 파일은 처리 시간이 길 수 있음
- 텍스트 길이 제한: 약 2000 토큰 (~1000 단어)

---

## 📊 모델 정보

현재 사용 중인 모델: **GPT-4o mini**
- 빠른 응답 속도
- 이미지 분석 지원
- 합리적인 비용

---

## 💡 팁

### 더 좋은 답변 받기
1. **명확한 질문**: "파이썬 리스트 정렬하는 방법"
2. **컨텍스트 제공**: 코드 파일, 관련 문서 첨부
3. **구체적인 요청**: "3가지 방법을 비교해줄 수 있어?"
4. **이미지 활용**: 캡처본이나 다이어그램 첨부

### 대화 관리
- 주제를 바꿀 때는 "대화 초기화" 사용
- 장시간 대화 후 맥락이 뒤바뀌면 초기화 후 재시작

---

## 📚 참고 자료

- [OpenAI API 문서](https://platform.openai.com/docs)
- [Streamlit 문서](https://docs.streamlit.io)
- [GPT-4 Vision 가이드](https://platform.openai.com/docs/guides/vision)

---

## 👨‍💻 개발자 정보

- **개발**: Claude Haiku 4.5
- **프로젝트**: streamlit-basic
- **마지막 업데이트**: 2025-09-17
