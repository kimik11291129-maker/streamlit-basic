[project]
name = "streamlit-basic"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "linker", email = "kimik11291129@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.5",
    "streamlit>=1.64.0",
    "openai>=1.0.0",
    "python-dotenv>=1.0.0",
    "pillow>=10.0.0",
]

[project.scripts]
streamlit-basic = "streamlit_basic:main"

[build-system]
requires = ["uv_build>=0.12.5,<0.13.0"]
build-backend = "uv_build"
